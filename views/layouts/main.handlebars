<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Let's Donate</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
</head>
<body>
    <nav class="navbar">
        <div class="nav-left">
            {{#if user}}
                {{#unless user.isAdmin}}
                    <a href="/">Home</a>
                    <a href="/about-us">About Us</a>
                    <a href="/donate-form">I want to Donate</a>
                {{/unless}}
            {{else}}
                <a href="/">Home</a>
                <a href="/about-us">About Us</a>
                <a href="/donate-form">I want to Donate</a>
            {{/if}}

        </div>
        <div class="nav-right">
            {{#if user}}
    {{#if user.isAdmin}}
        <a href="/my-account" class="btn"><span>Hi Admin {{user.firstname}}!</span></a>
        <a href="/admin" class="btn"><span>Dashboard</span></a>
        <div class="dropdown">
            <a href="#" class="btn">My Activity <span class="dropdown-icon">▼</span></a>
            <div class="dropdown-content">
                <a href="/admin/users">Manage User</a>
                <a href="/admin/donations">Manage Donation</a>
                <a href="/logout">Logout</a>
            </div>
        </div>
    {{else}}
        <a href="/my-account" class="btn"><span>Hi {{user.firstname}}!</span></a>
        <div class="dropdown">
            <a href="#" class="btn">My Activity <span class="dropdown-icon">▼</span></a>
            <div class="dropdown-content">
                <a href="/mydonations">My Donations</a>
                <a href="/mycart">My Bucket</a>
                <a href="/messages">Messages</a>
                <a href="/my-account">My Account</a>               
                <a href="/logout">Logout</a>
            </div>
        </div>
        <a href="/mycart" class="btn"><i class="fas fa-shopping-basket"></i></a>
    {{/if}}
            {{else}}
                <a href="/basket" class="btn">Basket</a>
                <a href="/login" class="btn">Sign In</a>
            {{/if}}
        </div>
    </nav>
    {{{body}}}

            <!-- Footer Section -->
<footer class="footer">
    <div class="footer-content">
        <p>&copy; 2025 HAMK-CA-group 18. All rights reserved.</p>
    </div>
</footer>
</body>
</html>
<script src="/js/script.js"></script>